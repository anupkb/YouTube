import TopButtonMenu from "./TopButtonMenu";
import VideoContainer from "./VideoContainer";

const MainContainer = () => {
  return (
    <div className="ml-52 flex-1 p-4">
      <TopButtonMenu />
      <VideoContainer />
    </div>
  );
};

export default MainContainer;
